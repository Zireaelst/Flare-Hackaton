# Agent instructions

See [CLAUDE.md](./CLAUDE.md). It is the single source of project context:
constraints, decisions already settled, and the traps that cost time to find.

Kept as a separate file because different tools look for different names; the
content lives in one place so the two cannot drift apart.
